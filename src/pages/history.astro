---
import Layout from "../layouts/Layout.astro";
import ssn782 from "../images/ssn782.jpg";

const entries = [
  {
    title: "Programming & Development",
    dateStr: "2020 to present",
    content: [
      "Dabbling with Microsoft Excel and Visual Basic was the beginning of my obsession with code. Since discovering my passion for solving problems through code, I quickly picked up the fundamentals of programming and created several projects in order to learn different concepts and technologies.",

      "My language experience includes proficiency in HTML/CSS/JavaScript, TypeScript and familiarity with Python. My portfolio also includes projects demonstrating my work with Vue and Astro.",
    ],
    imgSrc: undefined,
  },
  {
    title: "Nuclear Operations, Maintenance & Training",
    dateStr: "2015 to 2023",
    content: [
      "I graduated from Naval Nuclear Power School in 2016 at the top of ny class.  I completed training in Charleston, South Carolina, qualifying as a Nuclear Electronics Technician and Reactor Operator. Completion of the nuclear training pipeline required an inexhaustive attention to detail and commitment to excellence.",

      "In Pearl Harbor, Hawaii, I served aboard the USS Mississippi for a six-month overseas deployment and a two-year dry-dock maintenance period. My time as a submariner has nurtured my problem-solving and leadership skills, as well as provided me with experience in project planning and management.",
    ],
    imgSrc: ssn782,
  },
];
---

<Layout title="History | Andre L. Hammons">
  <section id="timeline">
    {
      entries.map((entry) => (
        <Fragment>
          <div class:list={["card", { "with-img": entry.imgSrc }]}>
            <h2 data-text={entry.dateStr}>{entry.title}</h2>
            <Fragment>
              <div class="content" data-img={entry.imgSrc}>
                {entry.content.map((para) => (
                  <p>{para}</p>
                ))}
              </div>
              {entry.imgSrc && <img src={entry.imgSrc} alt="" />}
            </Fragment>
          </div>
        </Fragment>
      ))
    }
  </section>
</Layout>

<style>
  #timeline {
    margin-top: 80px;
    color: var(--color1);
  }

  .card {
    margin: 0 10%;
    padding-top: 2em;
    display: flex;
    flex-direction: column;

    &.with-img {
      @media (min-width: 800px) {
        display: grid;
        gap: 0.5em;
        grid-template-columns: 40% 1fr;
        grid-template-areas:
          "title title"
          "text img";
      }
    }

    & .content {
      grid-area: text;
      container-type: inline-size;
      container-name: content;
      text-align: left;
      letter-spacing: 0.1em;
      padding: 0.5em;
      font-size: clamp(16px, 1.5cqw, 20px);

      & p {
        padding-top: 1em;
        line-height: 1.5;
      }
    }

    & h2 {
      grid-area: title;
      position: relative;
      font-size: clamp(18px, 2.5vw, 28px);
      text-align: left;
    }

    & h2::before {
      content: attr(data-text);
      display: block;
      position: absolute;
      right: 0;
      top: 100%;
      font-size: smaller;
      font-weight: 300;
      color: var(--accent3);
    }

    & img {
      grid-area: img;
      max-width: 100%;
      padding-top: 2em;
    }
  }
</style>
