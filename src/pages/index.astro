---
import Layout from "../layouts/Layout.astro";
import showcaseImg from "../images/home-img-muted.webp";
---

<Layout title="About | Andre L. Hammons">
  <main>
    <section>
      <div id="showcase">
        <img src={showcaseImg} alt="Andre visiting Kyoto, Japan" />
      </div>
      <div id="about">
        <h2></h2>
        <p>
          Andre served as a Nuclear
          Electronics Technician for eight years in Charleston, SC and Pearl
          Harbor, HI. During his service, he discovered his passion for
          coding and a strong interest in software development. Dedicated to
          life-long learning, Andre applies his experience gained through his
          naval service to a career in software development.
        </p>
      </div>
    </section>
  </main>
</Layout>

<style>
  main {
    height: clamp(400px, 100vh, 800px);
    display: flex;
    flex-direction: column;
  }

  section {
    @media (width >= 680px) {
      margin-top: 15%;
      display: grid;
      grid-auto-rows: max-content;
      grid-template-columns: 40% 1fr;

      & #showcase {
        padding-top: 0;
        padding-left: 5%;
        margin-top: -10%;
      }
    }
  }

  #showcase {
    width: clamp(300px, 100%, 800px);
    overflow: hidden;
    margin: 0 auto;
    padding-top: 15%;
    position: relative;
    z-index: 0;

    /* dark overlay */
    &::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(180deg, var(--color2) 20%, transparent 20%);
      z-index: 1;
    }
  }

  #about {
    background-color: var(--color2);
    color: var(--color1);
    padding: 1.5em 10%;

    & p {
      text-align: left;
      letter-spacing: 0.1em;
      font-size: clamp(16px, 2vw, 20px);
    }

    & h2 {
      text-align: center;
      margin-bottom: 0.5rem;
      letter-spacing: 0.1em;
    }
  }

  img {
    width: inherit;
    max-width: 680px;
    z-index: 0;
  }
</style>
